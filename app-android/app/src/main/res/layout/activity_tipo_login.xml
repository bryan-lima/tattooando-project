<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".TipoLoginActivity">

    <Button
        android:id="@+id/tip_log_btn_cliente"
        style="Widget.MaterialComponents.Button.Icon"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="50dp"
        android:layout_marginLeft="50dp"
        android:layout_marginTop="204dp"
        android:layout_marginEnd="60dp"
        android:layout_marginRight="60dp"
        android:text="@string/sou_cliente"
        android:textColor="@android:color/white"
        android:textSize="26sp"
        app:backgroundTint="?attr/colorPrimary"
        app:elevation="2dp"
        app:icon="@drawable/ic_perfil"
        app:iconGravity="start"
        app:iconPadding="4dp"
        app:iconSize="24dp"
        app:iconTint="@android:color/white"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.497"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:strokeWidth="0dp" />

    <Button
        android:id="@+id/tip_log_btn_studio"
        style="Widget.MaterialComponents.Button.Icon"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="50dp"
        android:layout_marginLeft="50dp"
        android:layout_marginTop="50dp"
        android:layout_marginEnd="60dp"
        android:layout_marginRight="60dp"
        android:text="@string/sou_studio"
        android:textColor="@android:color/white"
        android:textSize="26sp"
        app:backgroundTint="?attr/colorPrimary"
        app:elevation="2dp"
        app:icon="@drawable/ic_logo_app_white_background_transp"
        app:iconGravity="start"
        app:iconPadding="4dp"
        app:iconSize="24dp"
        app:iconTint="@android:color/white"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.508"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/tip_log_btn_cliente"
        app:strokeWidth="0dp" />

</androidx.constraintlayout.widget.ConstraintLayout>